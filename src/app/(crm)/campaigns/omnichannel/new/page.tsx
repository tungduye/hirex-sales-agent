import { NewChannelCampaignForm } from "@/modules/channels/components/new-channel-campaign-form"; import { getChannelCampaignOptions } from "@/modules/channels/server/get-channel-campaign-data";
export default async function NewOmnichannelCampaignPage(){const options=await getChannelCampaignOptions();return <div className="mx-auto max-w-4xl space-y-5"><header><h1 className="text-3xl font-bold">New omnichannel campaign</h1><p className="text-sm text-slate-500">Create a draft with one initial step. Launch remains a separate reviewed action.</p></header><NewChannelCampaignForm contacts={options.contacts as never} senders={options.senders as never}/></div>}
