import { notFound } from "next/navigation"; import { getChannelCampaign } from "@/modules/channels/server/get-channel-campaign-data";
export default async function OmnichannelCampaignDetail({params}:{params:Promise<{campaignId:string}>}){const{id}= {id:(await params).campaignId};const data=await getChannelCampaign(id);if(!data)notFound();return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">Omnichannel campaign</p><h1 className="text-3xl font-bold">{data.campaign.name}</h1><p className="text-sm text-slate-500">{data.campaign.status}</p></header><section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Sequence steps</h2>{data.steps.map(step=><div key={step.id} className="mt-3 rounded-lg border p-3 text-sm">Step {step.position} · delay {step.delay_minutes} min · {step.allowed_channels.join(", ")}</div>)}</section><section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Recipients</h2><p className="mt-2 text-sm text-slate-500">{data.recipients.length} recipients</p></section><section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Timeline</h2>{data.events.map(event=><p key={event.id} className="mt-2 text-sm">{event.event_type} · {new Date(event.created_at).toLocaleString()}</p>)}</section></div>}
