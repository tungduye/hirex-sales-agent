import Link from "next/link"; import { listChannelCampaigns } from "@/modules/channels/server/get-channel-campaign-data";
export default async function OmnichannelCampaignsPage(){const rows=await listChannelCampaigns();return <div className="space-y-6"><header className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">Omnichannel</p><h1 className="text-3xl font-bold">Channel campaigns</h1></div><Link href="/campaigns/omnichannel/new" className="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">New campaign</Link></header><div className="divide-y rounded-xl border bg-white">{rows.map(row=><Link key={row.id} href={`/campaigns/omnichannel/${row.id}`} className="flex justify-between p-4 hover:bg-slate-50"><span className="font-semibold">{row.name}</span><span className="text-sm text-slate-500">{row.status}</span></Link>)}{rows.length===0?<p className="p-10 text-center text-sm text-slate-500">No omnichannel campaigns yet.</p>:null}</div></div>}
