import { getChannelDeliveryReport } from "@/modules/channels/server/get-channel-delivery-report";
export default async function AnalyticsPage(){const data=await getChannelDeliveryReport();const cards=[['Actions',data.report.actions],['Accepted',data.report.accepted],['Delivered',data.report.delivered],['Read',data.report.read],['Clicked',data.report.clicked],['Replied',data.report.replied],['Failed',data.report.failed],['Unknown',data.report.unknown]];return <div className="space-y-6"><header><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">Live data</p><h1 className="text-3xl font-bold">Omnichannel analytics</h1><p className="text-sm text-slate-500">Provider events are deduplicated per outbound action. Missing provider capabilities are not inferred.</p></header>{data.error?<div className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">{data.error}</div>:null}<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label,value])=><div key={label} className="rounded-xl border bg-white p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</div><section className="rounded-xl border bg-white"><div className="border-b px-5 py-4 font-semibold">By channel</div>{data.byChannel.map(row=><div key={row.channelType} className="grid grid-cols-5 gap-3 border-b px-5 py-3 text-sm"><strong>{row.channelType}</strong><span>{row.actions} actions</span><span>{row.sent} sent</span><span>{row.failed} failed</span><span>{row.unknown} unknown</span></div>)}{data.byChannel.length===0?<p className="p-8 text-center text-sm text-slate-500">No channel delivery data yet.</p>:null}</section></div>}
