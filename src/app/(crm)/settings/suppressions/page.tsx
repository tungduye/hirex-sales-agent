import { PageHeader } from "@/components/shared/page-header";
import { setEmailSuppression, removeEmailSuppression } from "@/modules/campaigns/server/campaign-actions";
import { getAccountContext } from "@/modules/identity/server/get-account-context";
import { createPrivilegedSupabaseClient } from "@/modules/integrations/gmail/server/privileged-supabase";

export default async function SuppressionsPage(){const account=await getAccountContext();const db=createPrivilegedSupabaseClient();const {data}=account?.workspaceId?await db.from("email_suppressions").select("normalized_email,reason,source,created_at").eq("workspace_id",account.workspaceId).order("created_at",{ascending:false}):{data:[]};return <><PageHeader title="Email suppressions" description="Addresses listed here cannot be claimed by a campaign worker."/><form action={setEmailSuppression} className="mb-5 flex max-w-xl gap-2"><input name="email" type="email" required placeholder="recipient@example.com" className="h-10 flex-1 rounded-lg border px-3"/><button className="rounded-lg bg-slate-900 px-4 text-sm text-white">Suppress</button></form><div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Email</th><th>Reason</th><th>Source</th><th></th></tr></thead><tbody className="divide-y">{(data??[]).map(row=><tr key={row.normalized_email}><td className="p-3">{row.normalized_email}</td><td>{row.reason}</td><td>{row.source}</td><td>{row.reason==="MANUAL"&&<form action={removeEmailSuppression}><input type="hidden" name="email" value={row.normalized_email}/><button className="text-blue-700">Unsuppress</button></form>}</td></tr>)}</tbody></table></div></>}
